{
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^8.0.12",
    "@rails/webpacker": "5.4.4",
    "jquery": "^3.7.1",
    "sass": "^1.84.0",
    "webpack-cli": "3.3.12"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "webpack-dev-server": "3"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --format=esm --outdir=app/assets/builds --public-path=/assets",
    "build:css": "esbuild app/javascript/stylesheets/*.scss --bundle --loader:.scss=css --outdir=app/assets/builds --public-path=/assets",
    "start": "esbuild app/javascript/*.* --bundle --format=esm --outdir=app/assets/builds --public-path=/assets --watch"
  }
}
